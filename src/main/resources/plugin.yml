name: SpawnManagementPlus
version: '${version}'

main: com.rayssmp.utilities.Main
api-version: '26.1.2'
load: POSTWORLD
commands:
  setfirstjoinlocation:
    description: Set the joins.first_join.action.location in the configuration file and updates in-memory information.
    usage: /setfirstjoinlocation
    permission: SpawnManagementPlus.setfirstjoinlocation